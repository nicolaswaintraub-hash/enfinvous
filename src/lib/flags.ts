/**
 * Beta mode. While true, the site hides all subscription, login and logout UI
 * (and the CTAs that funnel into them) so the beta reads as a pure showcase.
 * Flip to false at public launch to restore the full conversion experience.
 */
export const BETA = true;
